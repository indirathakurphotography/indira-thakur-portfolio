'use client';

/**
 * The browser favicon is served from the dedicated, square public icon assets.
 * A rectangular branding upload is suitable for social previews, but at 16px a
 * browser reduces it to an unreadable glyph. Keep those responsibilities apart.
 */
export default function DynamicHead() {
  return null;
}
