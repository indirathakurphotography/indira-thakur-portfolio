'use client';
import { useEffect,useState } from 'react';
const categories=['home','newborn','maternity','portrait','wedding','events','brand'];
type Item={_id:string;title:string;category:string;mediaType:'instagram'|'video';url:string;thumbnailUrl?:string};
export default function InstagramLinksAdmin(){const [items,setItems]=useState<Item[]>([]);const [saving,setSaving]=useState(false);const [form,setForm]=useState({title:'',category:'home',mediaType:'instagram' as 'instagram'|'video',url:'',thumbnailUrl:''});const load=()=>fetch('/api/instagram-links').then(r=>r.json()).then(setItems);useEffect(()=>{load()},[]);const save=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);await fetch('/api/instagram-links',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setSaving(false);setForm({title:'',category:'home',mediaType:'instagram',url:'',thumbnailUrl:''});load()};const upload=async(file:File)=>{const data=new FormData();data.append('file',file);data.append('folder','instagram');const r=await fetch('/api/upload',{method:'POST',body:data});const json=await r.json();const uploadedUrl=json.url||json.src;if(uploadedUrl)setForm(f=>({...f,url:uploadedUrl,mediaType:'video'}))};return <div className="max-w-5xl mx-auto p-6 md:p-10"><h1 className="font-serif text-3xl text-[#2B2625]">Instagram Links</h1><p className="text-sm text-[#7C706D] mt-2">Add Instagram URLs or upload direct videos for the homepage and gallery categories.</p><form onSubmit={save} className="mt-8 grid gap-4 bg-white border border-[#E7DDD2] p-6"><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Title (optional)" className="border p-3"/><div className="grid grid-cols-2 gap-4"><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="border p-3">{categories.map(c=><option key={c}>{c}</option>)}</select><select value={form.mediaType} onChange={e=>setForm({...form,mediaType:e.target.value as 'instagram'|'video'})} className="border p-3"><option value="instagram">Instagram URL</option><option value="video">Direct video</option></select></div><input value={form.url} onChange={e=>setForm({...form,url:e.target.value})} placeholder="Instagram URL or uploaded video URL" className="border p-3" required/>{form.mediaType==='video'&&<input type="file" accept="video/*" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/>}<input value={form.thumbnailUrl} onChange={e=>setForm({...form,thumbnailUrl:e.target.value})} placeholder="Video thumbnail URL (optional)" className="border p-3"/><button disabled={saving} className="bg-[#2B2625] text-white px-5 py-3">{saving?'Savingâ€¦':'Add Instagram item'}</button></form><div className="mt-10 space-y-3">{items.map(item=><div key={item._id} className="border border-[#E7DDD2] p-4 flex justify-between gap-4"><div><b>{item.title||'Untitled'}</b><p className="text-xs text-[#7C706D]">{item.category} Â· {item.mediaType} Â· {item.url}</p></div><button onClick={async()=>{await fetch('/api/instagram-links?id='+item._id,{method:'DELETE'});load()}} className="text-red-600">Delete</button></div>)}</div></div>}

